<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >
    <item android:id="@+id/home_menu_following_tags"
        android:icon="@drawable/ic_menu_following_tag"
        android:title="@string/home_menu_following_tags_text"
        app:showAsAction="always"
        />
    <item android:id="@+id/home_menu_tags"
        android:icon="@drawable/ic_menu_tag"
        android:title="@string/home_menu_tags_text"
        app:showAsAction="always"
        />
    <item android:id="@+id/home_menu_search"
        android:icon="@drawable/ic_menu_search"
        android:title="@string/home_menu_search_text"
        app:showAsAction="always"
        />

</menu>
